
import { _Header, _Inner } from "./styles";
const Header = (props) => {
    return (
        <_Header>
            <_Inner>
                Header
            </_Inner>
        </_Header>
    )
}

export default Header;